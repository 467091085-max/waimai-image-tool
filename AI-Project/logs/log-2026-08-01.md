# Execution Log

## 2026-08-01

- Read `AI-Project/state/current.md` and the relevant background, taxonomy,
  selected-background, and Render decisions before work.
- Kept the original dirty Worktree untouched and continued in the isolated
  `codex/render-image-staging` clone at commit `8f9afd6`.
- Queried the protected Render staging readiness endpoint. TokenHub
  `hy-image-v3.0` and Tencent COS reported ready.
- Uploaded the real 56-row light-food Excel menu; parsing returned zero errors.
- Ran one paid background request and a frontend-equivalent two-at-a-time six
  request probe. All six returned HTTP 200 with TokenHub assets.
- Downloaded all six outputs and created
  `/tmp/waimai-light-food-backgrounds-contact.jpg` for visual review.
- Rejected the six-image set: several outputs contain raised rectangular
  plinths or slab-like blocks. Transport success is not visual acceptance.
- Built a 34-file sanitized source package at
  `/tmp/waimai-background-architecture-8f9afd6db1ea.zip`; size 303,524 bytes;
  SHA-256 `5394b3f88d938412ea37575112e4e40e5882c27283ca32a51b181a0a819825a6`.
  High-risk key/token/private-key pattern scan found no candidates.
- Started three separate ChatGPT Pro reviews for catalog architecture,
  provider/UI failure semantics, and the 25-category visual prompt system.
- Located a deterministic customer-browser failure after the successful API
  response: `privateMediaObjectUrl()` rejected every private image when no
  product Bearer token existed, even though the protected staging site uses
  valid same-origin Basic Auth. The caller then mislabeled that media load
  exception as `ProviderError`.
- Updated `static/app.js` to permit the signed private-media request to use
  same-origin credentials, attach Bearer only when present, preserve server
  authorization, and classify private-media failures separately from Hunyuan.
- Updated the customer UI contract tests; focused verification passed with
  `27 passed`.
- Full regression after the staging private-media fix passed with
  `1284 passed, 20 skipped`.
- Committed and pushed the staging-only fix as `d6cbf15`; Render auto-deployed
  that commit and reported it live.
- Ran a real headless Chrome flow with Render Basic Auth and no product Bearer
  token. Uploading the 56-row Excel produced six background cards, six blob
  images, no provider/media failure copy, and no browser console errors.
- Saved `/tmp/waimai-staging-private-media-fix.png`. Visual inspection still
  rejects the current background set: it includes raised plinths and forbidden
  cup, flowers, and cloth props. The transport/UI bug is fixed; background
  content quality is a separate open blocker.
- Next action: independently review Pro outputs, implement the smallest catalog
  foundation and truthful failure contract, then regenerate and visually accept
  one category before any 25-category paid batch.
- Added `background_categories.py` as the stable 25-category background layer;
  the existing 40-leaf dish taxonomy remains unchanged for product matching.
- Wired the background category, label, confidence, source, and evidence into
  `menu_background_context()` without removing its existing taxonomy fields.
- Added focused coverage for all 25 public labels, all 40 taxonomy mappings,
  light food, pasta, vermicelli soup, dry pot, conflicting menus, and combo
  components. Verification passed: `13 passed`.
- Product direction superseded that uncommitted 25-category experiment. Removed
  the intermediate layer and restored the existing 40 leaf taxonomies as the
  canonical background catalog keys. The catalog target is now 240 approved
  assets: 40 categories times six fixed style slots.
- Sent the 40-category correction to the ChatGPT Pro architecture review. The
  reliability review remains valid; the visual review also requires this new
  category count before its output can be accepted.
- Added `background_catalog.py` with exactly 40 taxonomy IDs, six fixed style
  slots, 240 expected pairs, immutable category/style/prompt/SHA COS keys, and
  fail-closed manifest readiness. `mixed` is rejected as a catalog category.
- Replaced prompt fragments that encouraged props with category-specific color,
  material, and light palettes only. The six slots are two seamless-solid sets
  plus four full-width flat-table sets; podiums, plinths, boards, trays, cloth,
  cups, plants, and picture-in-picture layouts are forbidden.
- Focused catalog, prompt, and taxonomy verification passed: `27 passed`.
- Added a shared-tenant PostgreSQL query that returns all approved records for
  an exact category/pipeline and preserves duplicates for fail-closed manifest
  validation. It does not perform name or cross-category fuzzy matching.
- Added `/api/background-catalog` and customer UI group loading. In approved-only
  mode, missing or duplicate slots, low-confidence classification, and catalog
  outages have distinct states and never trigger synchronous provider fallback.
- Added the resumable `scripts/build_background_catalog.py` operator workflow.
  It plans exactly 240 assets and can generate, quality-check, upload with COS
  read-back, register pending shared assets, and persist six-slot manifests.
- Focused runtime/catalog/UI verification passed: `114 passed, 2 skipped`.
- Audited 24 real local Excel menus against the revised confidence rule. All 24
  parsed and classified to an explicit existing leaf taxonomy; none fell into
  `mixed`. The light-food workbook resolved to `light_food` from explicit store
  taxonomy evidence with confidence 96.
- Added operator-builder contract tests covering the exact 240-asset default
  plan, prompt-bound local resume metadata, immutable shared object upload and
  read-back, and pending-only asset registration. Focused catalog/builder
  verification passed: `12 passed`.
- Full regression after the 40-category catalog foundation passed
  `1299 passed, 20 skipped`. Scoped Python compilation, frontend JavaScript
  syntax, `git diff --check`, and the 240-asset dry plan all passed.
- Independent source review found that a category keyword in the store name
  could previously override strongly conflicting evidence from the full menu.
  Added a fail-closed corroboration gate: store/file agreement or nonconflicting
  menu evidence may select the store taxonomy; an unsupported strong conflict
  returns `mixed/review` instead of choosing the wrong catalog.
- The new conflict regression passed, and all 24 real Excel menus still resolve
  to explicit taxonomies with zero `mixed` results. Full regression after the
  classification correction passed `1300 passed, 20 skipped`.
- Saved the exact 40 IDs, six slots, immutable object-key contract, review
  states, pilot command, rejection conditions, and rollout sequence in
  `AI-Project/handoffs/2026-08-01/BACKGROUND_CATALOG_40X6.md`.
- Render reported deploy `919ad29` live. Its Free plan does not provide Shell,
  and the service environment has private COS/TokenHub settings but no
  PostgreSQL runtime variables.
- Added `--upload-pending` so the operator builder can upload exact generated
  objects plus a pending category manifest to COS without falsely claiming a
  database registration.
- Added an explicit object-storage manifest backend for this test deployment.
  It requires top-level and per-asset approval, all six exact slots, current
  prompt hashes, immutable object keys, SHA-256 and bounded file sizes. Pending,
  missing, duplicate, malformed, or tampered entries fail closed.
- COS catalog/builder focused verification passed `16 passed`.
- Full regression after the COS manifest backend passed
  `1304 passed, 20 skipped`.
- All three ChatGPT Pro reviews delivered after a bounded wait. Saved their
  links, source identity, accepted findings, rejected suggestions and evidence
  limits in `AI-Project/handoffs/2026-08-01/CHATGPT_PRO_BACKGROUND_REVIEWS.md`.
- Accepted the visual review's camera, safe-area, coherent-light and phantom
  shadow constraints. Rejected its incomplete 25-item YAML and generic
  category-free prompt recommendation because those contradict the exact
  40-category requirement.
- Bumped the catalog prompt/pipeline to `style-background.v9`; category-specific
  palettes remain, while all six slots now state the placement geometry and
  lighting constraints. Focused verification passed `61 passed, 1 skipped`.
- Full regression after v9 prompt hardening passed
  `1304 passed, 20 skipped`.
- Generated the real `light_food` v9 six-slot pilot on the authorized Render
  test service and uploaded all six exact objects plus a pending manifest to
  private COS. Upload read-back hashes matched the generated bytes.
- Inspected all six exact COS previews. Styles 1, 2, and 4 contain raised
  display plinths; style 5 contains a white rectangular mat; styles 3 and 6
  are continuous table surfaces. Rejected the category and left every asset
  pending; no customer manifest was approved.
- Located the prompt conflict: `承载安全区`, post-composition, and commercial
  product-display wording outweighed the negative prompt and induced a stage.
- Added prompt v10 with one continuous surface, center texture continuity,
  table-front-edge-outside-crop, no second plane, and stronger mat/color-block
  prohibitions. Updated prompt-bound catalog contracts without changing the 40
  category IDs or six style slots.
- Prompt/catalog focused verification passed `61 passed, 1 skipped`; complete
  local regression passed `1304 passed, 20 skipped`; Python compilation and
  `git diff --check` passed.
- Generated and uploaded the real `light_food` v10 six-slot pilot. All six
  provider calls, local quality checks, immutable COS writes, and SHA read-back
  checks passed; Render was immediately restored to its normal Gunicorn start
  command and deploy `dep-d9mtp1u1egvs73evb0ng` is live.
- Rejected the v10 visual set. It removed the earlier small plinths, but style 2
  contains overlapping paper cyclorama sheets and styles 3, 4, and 6 expose
  prominent table-front planes or slabs. No v10 asset was approved.
- Rechecked Tencent's current Hunyuan 3.0 API contract. `Revise` defaults on;
  fixed seeds do not apply while rewriting is on; v3 documents `Revise` and
  `Seed` but not `NegativePrompt`.
- Added prompt v11 and a model-aware TokenHub payload. Offline catalog calls now
  send `Revise=0` and a deterministic positive seed for every category/style;
  Lite retains `negative_prompt`, while v3 no longer sends the unsupported
  field. Solid slots now use a single category color and architectural seamless
  material instead of multi-color paper wording.
- Prompt v11 provider/catalog/profile verification passed `91 passed, 1
  skipped`; full regression passed `1306 passed, 20 skipped`; scoped Python
  compilation and `git diff --check` passed. A new paid v11 pilot remains
  pending.
- Pushed commit `f6b216d` to `codex/render-image-staging`; Render auto-deploy
  `dep-d9mtuiht0dsc738mgbi0` became live.
- Generated the real v11 `light_food` pilot with prompt rewriting disabled and
  deterministic seeds. All six completed and uploaded with these exact hashes:
  style 1 `2c8a2644609a73191f0b605210a8defcc87d9954f6f08fc3b423e7d189d02673`;
  style 2 `7d4ebf83b7b39606393284ca70c9f9ef40a6654a32ef1b7c7463aa3c17de2cb4`;
  style 3 `d0af5a7981b5baaf6433f2ec8ade9a04efad21ee9d2b192d15e8a71cce12bc20`;
  style 4 `198c0a8c167958dd84b2810fa9d9cac3d2fa937bf2a65de75fdac7c38206c2b5`;
  style 5 `7bf3d33ceb883e4a01ebd338ee200b7d4b2a9a769b8c6ab6e328fec21bbc657a`;
  style 6 `61516e0d3645c0f01a834273d99cf26edc33a390c6ef12b08ecec64331c7e3e6`.
- Inspected every exact COS preview. Styles 1 and 2 are empty single-color
  seamless spaces; styles 3 through 6 are ordinary full-size dining tables.
  None contains food, props, paper rolls, mats, small product plinths, inset
  pictures, frames, or blurred outer backgrounds. The six-slot set passes the
  product visual gate, while still remaining pending until hash-locked approval.
- Restored the service to the normal Gunicorn command immediately after the
  pilot. Restore deploy `dep-d9mu195aeets73aq62b0` is live.
- Added remote paid-work resume. It accepts a completed current-version COS
  manifest only after re-reading all six objects and matching category, style,
  prompt hash, immutable key, file size, bytes, and SHA-256; otherwise the
  provider path remains active.
- Moved manifest persistence into the category loop so each sixth image becomes
  a durable restart checkpoint. A 3 x 2 SHA-addressed review contact sheet is
  uploaded with every completed category.
- Added `scripts/review_background_catalog.py`. Approval requires six explicit
  reviewed hashes and writes reviewer/time/note metadata with COS read-back and
  post-write validation. Any hash mismatch fails before approval.
- Catalog/resume/review focused tests passed `98 passed, 1 skipped`; full
  regression passed `1313 passed, 20 skipped`.
- Deployed resumable catalog commit `b077584` as Render deploy
  `dep-d9mu79rbc2fs73dv8sig`.
- Ran the hash-locked review command against the six exact `light_food` v11
  hashes. It re-read every COS object, created review contact sheet SHA
  `45170ad94f6665d1c5750c86ce60f5284443bdfc63b5fddb3bb3de5947c6e768`,
  wrote reviewer/time metadata, read the manifest back, and reported
  `reviewStatus=approved` at `2026-08-01T12:19:51Z`.
- Restored normal Gunicorn start command after approval. Render deploy
  `dep-d9mu95daeets73aqmh8g` is live.
- Started resumable Render batch deploy `dep-d9muan2jnfac739vofpg` for
  `topped_rice`, `mixed_rice`, `porridge_soup_rice`, `rice_noodles`, and
  `wheat_noodles`. Gunicorn reached live state while the offline builder runs
  in the background. COS correctly reported no existing current-version
  manifests for all five categories, then real generation began.
- First observed batch checkpoint: `topped_rice/style-1` SHA-256
  `b7d038191ebe94cde15e570dc0237e126f6933ef1fb6600c93233a61cedac744`
  and `topped_rice/style-2` SHA-256
  `94fa03b0fa4e5f115a24efb5284c50294e5ec2e204f3ee09dd7049317b8030d6`
  both passed generation, immutable COS upload, and read-back verification.
  They remain pending until the complete six-image category is visually
  reviewed and hash-lock approved.
- Corrected a monitoring mistake: the Render deploy log viewer had been left
  on a fixed 05:22:56-05:25:38 PDT custom range, so later progress was hidden
  even though the builder continued normally. Switching the viewer to Live
  tail showed the complete batch; this was not a provider stall.
- First five-category batch finished successfully at 05:33:20 PDT:
  `complete=true`, `completedAssetCount=30`, `failureCount=0`. Completed
  categories are `topped_rice`, `mixed_rice`, `porridge_soup_rice`,
  `rice_noodles`, and `wheat_noodles`; each has a pending manifest and
  SHA-addressed review contact sheet in private COS.
- Tencent TokenHub console independently showed `hy-image-v3.0` in running
  state with zero free images remaining and per-image postpaid billing active.
  This confirms the paid provider is enabled; zero free allowance is not a
  not-configured or no-balance condition.
- Inspected the exact 3-by-2 COS contact sheets for all five first-batch
  categories. `topped_rice` and `mixed_rice` passed: clean empty seamless
  spaces plus ordinary full-size tables, with no food, props, plinths, insets,
  or blurred frames. `rice_noodles` failed because styles 3 through 6 used
  segmented color-block tabletops and style 5 added a second surface.
  `porridge_soup_rice` and `wheat_noodles` remain pending because style 2
  became a large decorative wood arch instead of a single-color seamless set.
- Hash-lock approved `topped_rice` at `2026-08-01T12:45:17Z` with review sheet
  `contact-sheet-26b64f77282da2e08d2fd5db67550246e5cbdec5b6ae2c01eb5664fbf7bb3397.jpg`.
  Hash-lock approved `mixed_rice` at `2026-08-01T12:45:47Z` with review sheet
  `contact-sheet-56f5da9419cb4f5d8c0a18dda2ea4265753e7e2505b82ed048200cd5c675db68.jpg`.
  Together with `light_food`, the approved catalog now contains 3 categories
  and 18 exact assets.
- Located the visual root cause in `pure_background_style_prompt()`: every
  table slot inherited a three-color category palette, encouraging patchwork
  tables, while the second seamless slot selected the last palette term, which
  could be a material-like value such as shallow or dark wood.
- Implemented profile v10 without invalidating approved assets. The exact 18
  v11 prompt hashes for `light_food`, `topped_rice`, and `mixed_rice` are
  regression-frozen. Unapproved table prompts now require one material and one
  color and prohibit patchwork, inlay, and color blocking; their second solid
  slot selects the second palette color. Focused prompt/catalog verification
  passed `28 passed`; maximum prompt length remains bounded at 588 characters.
- Complete regression for the profile v10 compatibility patch passed `1315
  passed, 20 skipped` in 20.65 seconds. Python compilation for the profile,
  builder, and reviewer modules passed, and `git diff --check` reported no
  whitespace errors.
- Regenerated `porridge_soup_rice`, `rice_noodles`, and `wheat_noodles` on
  isolated Render staging deploy `dep-d9muns3m8hqs73d6uh8g`. The run completed
  all `18/18` assets with zero failures while Gunicorn remained live.
- Inspected the three new SHA-addressed COS contact sheets. The earlier wood
  arches, segmented color-block tabletops, and second planes are gone. All 18
  images are empty usable backgrounds with either a simple seamless scene or
  one coherent full-size table; none contains food, props, small plinths,
  paper, inset frames, blurred outer images, or mixed-material tabletops.
- Hash-lock approved `porridge_soup_rice` at `2026-08-01T13:02:42Z`,
  `rice_noodles` at `2026-08-01T13:03:11Z`, and `wheat_noodles` at
  `2026-08-01T13:03:39Z` through deploy `dep-d9musljl550s7390l4jg`. Each
  approval re-read all six exact COS objects and the written manifest. The
  catalog checkpoint is now 6 approved categories and 36 approved assets.
- Second five-category Render batch `dep-d9muuj2jnfac73a13dhg` completed
  `29/30` assets. Four complete categories have pending manifests:
  `dumpling_wonton`, `buns_dim_sum`, `chinese_wraps`, and `malatang_maocai`.
  `hotpot_skewers` completed five slots; style 2 failed because TokenHub
  returned `FailedOperation.ImageIllegalDetected` for the fixed-seed image.
- Root cause: all three paid retries reused the identical category/style seed,
  so a seed-specific content-filter rejection could never recover. Added
  deterministic attempt-scoped retry seeds while preserving the exact first
  seed. Also removed v3-only `Revise` and `Seed` before legacy Tencent fallback.
- Focused generation verification passed `36 passed`; complete regression
  passed `1317 passed, 20 skipped` in 20.44 seconds. Scoped Python compilation
  and `git diff --check` passed.
- Pushed retry commit `312d578` to `codex/render-image-staging`; Render deploy
  `dep-d9mv5f3m8hqs73cjh8u0` became live. The original dirty worktree remained
  untouched.
- Visually reviewed the exact COS contact sheets for `dumpling_wonton`,
  `buns_dim_sum`, `chinese_wraps`, and `malatang_maocai`. All 24 assets are
  empty full-frame backgrounds without food, props, plinths, inset frames,
  blurred outer frames, patchwork tabletops, or second display planes.
- Hash-lock approved those four categories at `2026-08-01T13:26:53Z`,
  `2026-08-01T13:27:25Z`, `2026-08-01T13:27:57Z`, and
  `2026-08-01T13:28:26Z`. The catalog checkpoint is 10 approved categories / 60
  approved assets.
- Started staging deploy `dep-d9mv7qfqj5pc73dsecig` to approve the four reviewed
  categories and generate `hotpot_skewers`, `barbecue`, `fried_chicken`,
  `burger_hotdog`, and `pizza` while Gunicorn remains live. The alternate-seed
  retry recovered the formerly filtered `hotpot_skewers/style-2` paid image.
- Exact COS contact sheets for `hotpot_skewers` and `barbecue` passed visual
  review. Their six assets remain pending until a later explicit six-hash
  approval operation. At the latest checkpoint `fried_chicken` was complete,
  `burger_hotdog` was in progress, and `pizza` had not yet started.
- Batch deploy `dep-d9mv7qfqj5pc73dsecig` finished at 06:38:17 PDT with
  `complete=true`, `completedAssetCount=30`, and `failureCount=0`.
- Exact COS review rejected three category sets despite successful provider
  calls. `fried_chicken/style-2` has a distinct gray floor under a red solid
  backdrop and style 5 exposes a table-front slab. `burger_hotdog/style-2`
  similarly splits the solid scene into yellow and gray planes. `pizza/style-2`
  contains a large curled white paper/cyclorama form and style 6 exposes a dark
  table-front bar. All three manifests remain pending.
- Decided to add a narrow operator-only selective regeneration path: replace
  only explicitly named rejected slots with deterministic revision seeds,
  retain and re-verify all other remote manifest objects, then write a new
  pending six-slot manifest and contact sheet. This avoids approving defects or
  paying to replace already accepted images.
- Implemented selective regeneration in `scripts/build_background_catalog.py`.
  It requires exactly one category, explicit style slots, `--upload-pending`,
  and a positive seed revision. It refuses approved manifests, verifies the
  complete pending remote manifest before generation, replaces only selected
  entries, and rebuilds the six-slot pending manifest/contact sheet from the
  new entries plus preserved hash-verified remote entries.
- Added regression coverage for deterministic revision seeds, one-slot
  replacement with five exact hashes preserved, transient field stripping, and
  approved-manifest refusal. Focused verification passed `16 passed`; full
  regression passed `1320 passed, 20 skipped` in 21.35 seconds. Scoped Python
  compilation and `git diff --check` passed.
- Deployed selective replacement commit `0811be5` as Render deploy
  `dep-d9mvhdajnfac73a2effg`. Hash-lock approved `hotpot_skewers` at
  `2026-08-01T13:47:17Z` and `barbecue` at `2026-08-01T13:47:47Z`, bringing the
  approved catalog to 12 categories / 72 assets.
- Real selective regeneration completed five exact paid slots with zero
  provider failures: `fried_chicken/style-2` SHA
  `606fc71cf2b54a4753a55d4a4f701eef03c5237d928ab63b1ae57636316b54be`,
  `fried_chicken/style-5` SHA
  `4462034a23d79f694565c5da96894d0e5338e5ef2883dcea77353df92922b0a9`,
  `burger_hotdog/style-2` SHA
  `28bda761c0827de65e59d2442b32e536e716bb8f28aa2a12243c5493768065ef`,
  `pizza/style-2` SHA
  `752152bb8836ddf7eb382a08d4ba9ee3178832a5d152eccf7c3f410a10f22dd0`,
  and `pizza/style-6` SHA
  `1bc852c88c5301dd5087c413ad1fdf010ffb7b20789afe3f5f87855b984d4b91`.
- COS visual review confirmed the replacement mechanism works and table output
  improved, but alternate seeds alone did not fix the repeated gray second
  floor under the red/yellow style-2 scenes. The prompt said both `唯一主色` and
  `辅色`, which is semantically contradictory for a one-color requirement.
- Added prompt profile v11: unapproved style-2 prompts now require the exact same
  hue on wall, architectural curve, and floor and forbid any second-color floor.
  The 12 already approved category prompts remain byte-for-byte frozen. Remote
  selective resume now tolerates an old prompt hash only for explicitly replaced
  slots while retaining full key, size, byte, and SHA verification for all six
  source objects.
- Profile-v11 and selective stale-prompt focused tests passed `29 passed`; full
  regression passed `1322 passed, 20 skipped` in 21.34 seconds. Scoped Python
  compilation passed, all generated prompts remain within the 620-character
  contract, and `git diff --check` passed.
- Selectively regenerated the remaining style-2 slots with the corrected
  single-hue prompt. New SHA-256 values are
  `863a74ed27c6d277e55f27a5edfb874f945da3022c1b696bcf2fd273815dd574`
  for `fried_chicken`,
  `66c03de87ce368116df7fc4051f5ec1e1acf17cfeafaf51fc0962dc6c261231f`
  for `burger_hotdog`, and
  `ed773fbbf93982594c3ff070e5980d000436c9a793fdb9fbf1d5d7f658bffece`
  for `pizza`.
- Inspected the exact latest COS contact sheets. All three six-slot groups now
  have continuous same-hue solid spaces plus ordinary full-size tables, with
  no second-color floor, curled paper, plinth, prop, food, or inset image.
- Render approval deploy `dep-d9mvpm2jnfac73a32hvg` hash-lock approved
  `fried_chicken` at `2026-08-01T14:04:59Z`, `burger_hotdog` at
  `2026-08-01T14:05:38Z`, and `pizza` at `2026-08-01T14:06:11Z`. Each command
  re-read the six exact COS objects and post-write manifest. The catalog is now
  15 approved categories / 90 approved assets.
- Deployed and ran five-category batch `dep-d9mvthnlk1mc73dgkn3g` for
  `sandwich_bagel`, `pasta_steak`, `japanese`, `korean`, and
  `southeast_asian`. It completed all 30 paid images with `failureCount=0` and
  wrote five category manifests plus SHA-addressed contact sheets.
- Exact COS visual review passed `pasta_steak`, `korean`, and
  `southeast_asian`. `sandwich_bagel/style-2` rendered `浅木` as a wood wall
  above a different floor, while `japanese/style-2` rendered `原木` as a wood
  wall plus a stepped second plane. The other 28 images passed.
- Added profile v12 material-label normalization for seamless solid slots.
  Material-like palette labels now become plain color terms before prompting;
  only the two rejected style-2 slots will be selectively regenerated.
- Render deploy `dep-d9n09knqj5pc73dulbg0` hash-lock approved
  `pasta_steak` at `2026-08-01T14:38:59Z`, `korean` at
  `2026-08-01T14:39:33Z`, and `southeast_asian` at
  `2026-08-01T14:40:13Z`. Each approval re-read all six exact COS objects and
  the written manifest. The catalog checkpoint is 18 approved categories / 108
  approved assets.
- The same deploy selectively regenerated `sandwich_bagel/style-2` with the
  normalized single-color prompt. The provider call, upload, manifest rebuild,
  and object verification passed with SHA-256
  `5dc9ee66d7393ab58be244351ef4f664dfd5ffab7ad19dc7a4fd0684ca40c598`.
  The category remains pending until its updated contact sheet passes visual
  review; `japanese/style-2` is still in its bounded provider attempt window.
- `japanese/style-2` also completed and rebuilt its remote manifest/contact
  sheet with SHA-256
  `975f47a90f630632d67d8d3deab7d40e98a032992d50a5f8733801ce2dc7fab4`.
- Exact visual review passed both updated six-slot contact sheets. The
  `sandwich_bagel` style-2 wood wall is now a continuous plain warm-beige
  seamless scene. The `japanese` style-2 wood wall and stepped plane are now a
  continuous plain warm-brown seamless scene. Neither contact sheet contains
  food, props, plinths, inset images, frames, or an extra display plane. Both
  complete manifests remain pending until their exact six-hash approval write.
- Render deploy `dep-d9n0fdoae00c73ak8pbg` hash-lock approved
  `sandwich_bagel` at `2026-08-01T14:51:12Z` and `japanese` at
  `2026-08-01T14:51:46Z`. The approval commands matched and re-read all 12
  reviewed COS object hashes, and both remote manifests now report
  `reviewStatus=approved`. The catalog checkpoint is 20 approved categories /
  120 approved assets.
- Added `sandwich_bagel` and `japanese` to the hash-locked prompt sets. A
  dedicated profile-v12 frozen subset keeps material-like color normalization
  active for those approved prompt bytes; a first naive freeze was rejected by
  tests because it would have reintroduced raw `浅木` and `原木` wording.
- Prompt-freeze focused verification passed `20 passed`; complete regression
  passed `1324 passed, 20 skipped` in 21.49 seconds. Scoped Python compilation,
  maximum prompt length `591`, and `git diff --check` passed.
- Restored the Render start command to normal Gunicorn. Deploy
  `dep-d9n0hbrl550s73936reg` is live; no approval or paid generation command
  remains in the persistent start command.
