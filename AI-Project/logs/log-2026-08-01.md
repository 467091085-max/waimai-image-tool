# Execution Log

## 2026-08-01

- Read `AI-Project/state/current.md` and the relevant background, taxonomy,
  selected-background, and Render decisions before work.
- Kept the original dirty Worktree untouched and continued in the isolated
  `codex/render-image-staging` clone at commit `8f9afd6`.
- Queried the protected Render staging readiness endpoint. TokenHub
  `hy-image-v3.0` and Tencent COS reported ready.
- Uploaded the real 56-row light-food Excel menu; parsing returned zero errors.
- Ran one paid background request and a frontend-equivalent two-at-a-time six
  request probe. All six returned HTTP 200 with TokenHub assets.
- Downloaded all six outputs and created
  `/tmp/waimai-light-food-backgrounds-contact.jpg` for visual review.
- Rejected the six-image set: several outputs contain raised rectangular
  plinths or slab-like blocks. Transport success is not visual acceptance.
- Built a 34-file sanitized source package at
  `/tmp/waimai-background-architecture-8f9afd6db1ea.zip`; size 303,524 bytes;
  SHA-256 `5394b3f88d938412ea37575112e4e40e5882c27283ca32a51b181a0a819825a6`.
  High-risk key/token/private-key pattern scan found no candidates.
- Started three separate ChatGPT Pro reviews for catalog architecture,
  provider/UI failure semantics, and the 25-category visual prompt system.
- Located a deterministic customer-browser failure after the successful API
  response: `privateMediaObjectUrl()` rejected every private image when no
  product Bearer token existed, even though the protected staging site uses
  valid same-origin Basic Auth. The caller then mislabeled that media load
  exception as `ProviderError`.
- Updated `static/app.js` to permit the signed private-media request to use
  same-origin credentials, attach Bearer only when present, preserve server
  authorization, and classify private-media failures separately from Hunyuan.
- Updated the customer UI contract tests; focused verification passed with
  `27 passed`.
- Full regression after the staging private-media fix passed with
  `1284 passed, 20 skipped`.
- Committed and pushed the staging-only fix as `d6cbf15`; Render auto-deployed
  that commit and reported it live.
- Ran a real headless Chrome flow with Render Basic Auth and no product Bearer
  token. Uploading the 56-row Excel produced six background cards, six blob
  images, no provider/media failure copy, and no browser console errors.
- Saved `/tmp/waimai-staging-private-media-fix.png`. Visual inspection still
  rejects the current background set: it includes raised plinths and forbidden
  cup, flowers, and cloth props. The transport/UI bug is fixed; background
  content quality is a separate open blocker.
- Next action: independently review Pro outputs, implement the smallest catalog
  foundation and truthful failure contract, then regenerate and visually accept
  one category before any 25-category paid batch.
- Added `background_categories.py` as the stable 25-category background layer;
  the existing 40-leaf dish taxonomy remains unchanged for product matching.
- Wired the background category, label, confidence, source, and evidence into
  `menu_background_context()` without removing its existing taxonomy fields.
- Added focused coverage for all 25 public labels, all 40 taxonomy mappings,
  light food, pasta, vermicelli soup, dry pot, conflicting menus, and combo
  components. Verification passed: `13 passed`.
- Product direction superseded that uncommitted 25-category experiment. Removed
  the intermediate layer and restored the existing 40 leaf taxonomies as the
  canonical background catalog keys. The catalog target is now 240 approved
  assets: 40 categories times six fixed style slots.
- Sent the 40-category correction to the ChatGPT Pro architecture review. The
  reliability review remains valid; the visual review also requires this new
  category count before its output can be accepted.
- Added `background_catalog.py` with exactly 40 taxonomy IDs, six fixed style
  slots, 240 expected pairs, immutable category/style/prompt/SHA COS keys, and
  fail-closed manifest readiness. `mixed` is rejected as a catalog category.
- Replaced prompt fragments that encouraged props with category-specific color,
  material, and light palettes only. The six slots are two seamless-solid sets
  plus four full-width flat-table sets; podiums, plinths, boards, trays, cloth,
  cups, plants, and picture-in-picture layouts are forbidden.
- Focused catalog, prompt, and taxonomy verification passed: `27 passed`.
- Added a shared-tenant PostgreSQL query that returns all approved records for
  an exact category/pipeline and preserves duplicates for fail-closed manifest
  validation. It does not perform name or cross-category fuzzy matching.
- Added `/api/background-catalog` and customer UI group loading. In approved-only
  mode, missing or duplicate slots, low-confidence classification, and catalog
  outages have distinct states and never trigger synchronous provider fallback.
- Added the resumable `scripts/build_background_catalog.py` operator workflow.
  It plans exactly 240 assets and can generate, quality-check, upload with COS
  read-back, register pending shared assets, and persist six-slot manifests.
- Focused runtime/catalog/UI verification passed: `114 passed, 2 skipped`.
- Audited 24 real local Excel menus against the revised confidence rule. All 24
  parsed and classified to an explicit existing leaf taxonomy; none fell into
  `mixed`. The light-food workbook resolved to `light_food` from explicit store
  taxonomy evidence with confidence 96.
- Added operator-builder contract tests covering the exact 240-asset default
  plan, prompt-bound local resume metadata, immutable shared object upload and
  read-back, and pending-only asset registration. Focused catalog/builder
  verification passed: `12 passed`.
- Full regression after the 40-category catalog foundation passed
  `1299 passed, 20 skipped`. Scoped Python compilation, frontend JavaScript
  syntax, `git diff --check`, and the 240-asset dry plan all passed.
- Independent source review found that a category keyword in the store name
  could previously override strongly conflicting evidence from the full menu.
  Added a fail-closed corroboration gate: store/file agreement or nonconflicting
  menu evidence may select the store taxonomy; an unsupported strong conflict
  returns `mixed/review` instead of choosing the wrong catalog.
- The new conflict regression passed, and all 24 real Excel menus still resolve
  to explicit taxonomies with zero `mixed` results. Full regression after the
  classification correction passed `1300 passed, 20 skipped`.
- Saved the exact 40 IDs, six slots, immutable object-key contract, review
  states, pilot command, rejection conditions, and rollout sequence in
  `AI-Project/handoffs/2026-08-01/BACKGROUND_CATALOG_40X6.md`.
- Render reported deploy `919ad29` live. Its Free plan does not provide Shell,
  and the service environment has private COS/TokenHub settings but no
  PostgreSQL runtime variables.
- Added `--upload-pending` so the operator builder can upload exact generated
  objects plus a pending category manifest to COS without falsely claiming a
  database registration.
- Added an explicit object-storage manifest backend for this test deployment.
  It requires top-level and per-asset approval, all six exact slots, current
  prompt hashes, immutable object keys, SHA-256 and bounded file sizes. Pending,
  missing, duplicate, malformed, or tampered entries fail closed.
- COS catalog/builder focused verification passed `16 passed`.
- Full regression after the COS manifest backend passed
  `1304 passed, 20 skipped`.
