# Execution Log

## 2026-07-31
- Deployed protected branch `codex/render-image-staging` to the independent Render test service; the production service was not changed.
- Configured the test service for TokenHub `hy-image-v3.0`, the existing private COS bucket, and one generation worker.
- Uploaded `/Users/guiguixiaxia/Documents/menus/运营数据_蔬适圈·中式轻食健康餐（万达店）.xlsx`; 56 rows parsed successfully and the menu background category resolved to `light_food`.
- Re-ran `style-1` with pure-background prompt v4. The request took 23.27 seconds, returned `TokenHubImageV3`, and produced a new SHA-256 `44317659b1c0918cef550544a7824798a1974bb9d3023247e57d11addcfa2d22`.
- The earlier two-second old-hash observation was not used as a code-change justification; the clean re-upload proved the current version regenerates correctly.
- Next: generate and visually inspect styles 2-6, then validate six free samples and a formal image probe.
- Generated styles 2-6 sequentially through TokenHub in 19-23 seconds each. All six v4 outputs were unique 1024x768 JPEGs whose downloaded SHA-256 values exactly matched the API metadata.
- Visual review accepted styles 1, 4, 5, and 6 as empty backgrounds. Style 2 contained berries and style 3 contained avocado and lettuce, so v4 failed the six-of-six pure-background criterion.
- Root cause for the two remaining visual violations: the positive scene text still named the food category while the prompt did not explicitly ban edible ingredients and produce.
- Implemented pure-background prompt v5: removed category-scene and bowl/container silhouette cues, explicitly prohibited edible produce and ingredients, and bumped both prompt and profile cache identities.
- Focused generation/background/security regression passed: 83 tests.
- Complete local regression passed: 1269 tests; 20 environment-gated tests skipped.
- Render deployed commit `1e96158`; the real 56-row menu was uploaded again and six v5 TokenHub backgrounds were generated in 19-27 seconds each with exact downloaded SHA-256 verification.
- Visual review accepted v5 styles 1, 2, and 4. Style 3 contained a glass and styles 5 and 6 contained vases, so v5 failed the six-of-six no-container criterion.
- The remaining root cause is semantic: allowing non-edible materials still permits decorative props. The next prompt revision must prohibit every prop and allow only the empty countertop and background wall.
- Implemented pure-background prompt v6. It prohibits every decorative prop, plant, vessel, utensil, edible item, person, logo, and text; only an empty countertop and background wall are allowed.
- v6 retains six material/color/light variants and the category material profile while replacing object-like `点缀`/`细节` wording with surface `配色`/`纹理` wording.
- v6 focused regression passed 83 tests; complete local regression passed 1269 tests with 20 environment-gated skips.
- Render deployed commit `f6c4120`; the same 56-row menu and six v6 TokenHub backgrounds were generated again with exact downloaded SHA-256 verification.
- Visual review accepted v6 styles 1-5 as empty real-photo tabletops. Style 6 rendered a plated salad, so v6 failed one of six.
- The remaining positive cue was the literal category label `轻食/沙拉商品`. v7 removes food-category names from the model prompt while preserving the category-specific material/color profile.
- v7 focused regression passed 83 tests; complete local regression passed 1269 tests with 20 environment-gated skips.
- Render deployed commit `573e8af`. A fourth real six-background run for the 56-row menu produced six unique, SHA-verified 1024x768 images; visual review passed all six as empty real-photo surfaces with no food, vessels, props, text, or color-block placeholders.
- Selected style 3 and generated sample 0, `黑椒牛排能量碗`. The 1024x768 PNG passed output SHA verification, selected background identity, outside-mask pixel preservation, and full-frame visual review.
- The successful free sample took 108.66 seconds. A two-request concurrency probe produced one 106.87-second success and one 8.25-second `provider_quota` failure, proving the configured provider cannot safely overlap paid generation calls.
- Started a separate ChatGPT Pro engineering review at `https://chatgpt.com/c/6a6cee10-d528-83e8-bf71-cedee173cc86` for a one-provider-call chroma foreground extraction fast path with strict validation and cloud-mask fallback.
- Implemented a default-off, single-TokenHub-call chroma foreground path. Local extraction validates cyan border color and dispersion, edge-connected background, subject area, bounding margins, and output mask; any validation failure falls back once to the existing Tencent cloud mask service.
- Bumped the exact-background pipeline cache version so legacy foreground/mask artifacts cannot be mixed with the new mode. Deterministic composition, selected-background identity checks, and persisted-output verification remain unchanged.
- Focused chroma/selected-background/product-asset verification passed `29 passed, 1 skipped`; the complete default regression passed `1277 passed, 20 skipped` in 20.69 seconds. Python compilation and `git diff --check` passed.
- Render deployed chroma fast-path commit `38a4a8c`, and the test-only environment now enables `EXACT_BACKGROUND_CHROMA_FAST_PATH=true`.
- A real background retry exposed Tencent TokenHub error `401008`: the 50-image free trial was exhausted while postpaid was disabled. The Tencent console confirmed the account was not out of balance; HY-Image-V3.0 postpaid was enabled and the service changed to running with per-image billing.
- The next real six-background run succeeded technically with exact download SHA-256 matches, but visual QA rejected style 3 because it contained a vase and plant; the other five were empty tabletops.
- Refined only style 3's positive scene from a generic white-gray minimal tabletop to an empty seamless studio cyclorama. Prompt SHA cache binding invalidates only style 3. Focused tests passed `34 passed`; full regression passed `1278 passed, 20 skipped`; `git diff --check` passed.
- Real six-sample visual review exposed a false-success mode in the local chroma fast path: selected-background identity passed, but three outputs retained large turquoise chroma shadows inside the accepted foreground mask.
- Added a fail-closed residual-chroma validator. It measures chroma-like regions connected to the foreground boundary and raises `chroma_spill_too_large`; the existing cloud Mask provider then handles that source. Small enclosed cyan details remain accepted.
- Bumped the exact-background pipeline to v3 so cached v2 masks and outputs cannot bypass the new validator. Focused verification passed `21 passed`; full regression passed `1280 passed, 20 skipped`; compilation and `git diff --check` passed.
- ChatGPT Pro's P1 review correctly identified the foreground JPEG 92 cache as an avoidable chroma-edge contamination source. `save_result_image()` now writes PNG only when the target explicitly requests `.png`, and the chroma algorithm cache target is now a lossless RGB PNG; other image outputs retain their previous JPEG behavior.
- A canonical pixel test proves the intermediate PNG survives save/reload byte-for-byte in RGB pixel space. Updated focused verification passed `22 passed`; full regression passed `1281 passed, 20 skipped`; compilation and `git diff --check` passed.
- Added a test-service in-process TokenHub single-flight lock at the provider-call boundary. The Render test service is fixed to one instance and one Gunicorn worker, so concurrent background requests now queue instead of overlapping the paid model and triggering the observed provider-quota failure.
- A four-request fake-provider concurrency test observed `max_in_flight == 1`. Provider/image focused verification passed `50 passed`; full regression passed `1282 passed, 20 skipped`; compilation and `git diff --check` passed. Multi-instance production still requires a distributed lease and remains out of scope for this test deployment.
- Render commit `111c99f` generated all six real backgrounds through the same two-request frontend concurrency shape. All six succeeded with TokenHub, all downloaded SHA-256 values matched API metadata, and visual review found no food, text, watermark, or placeholder color blocks.
- The v3 six-sample run produced six technically successful outputs in 43-63 seconds each. Three suspicious sources correctly fell back to cloud Mask and looked clean; two local-mask outputs looked clean; `煎蛋` still had a visible cyan halo despite exact selected-background checks.
- The rejected `煎蛋` output measured residual chroma at `0.010104` of the image and `0.054984` of its foreground. Accepted local-mask samples measured about `0.0012` of the image, providing a clear calibration gap.
- Tightened the fail-closed threshold from 1.2%/4% to 0.4%/2% and bumped chroma extraction to v3 plus exact-background pipeline to v4 so no v3 mask/output cache can bypass the change. Added a sub-one-percent halo regression. Focused verification passed `51 passed`; full regression passed `1283 passed, 20 skipped`; compilation and `git diff --check` passed.
