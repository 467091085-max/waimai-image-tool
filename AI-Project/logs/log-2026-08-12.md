# Execution Log

## 2026-08-12

- Inspected the current Render staging deployment and reproduced a real
  60-row mixed-rice menu upload.
- Render logs showed all six free samples failed after foreground generation at
  local Chroma mask validation; menu parsing, taxonomy classification, COS
  background reads, and provider configuration had already succeeded.
- Root cause: `product-image-compiler.v1` requested both the selected scene and
  a uniform cyan screen in the same Chroma prompt.
- Updated the compiler to v2, Chroma prompt version to 6, preserved extractor
  reason codes in logs, and added contradiction regression coverage.
- Verification: focused suite passed `50 passed`.
- Full regression after installing the repository-declared Redis dependency:
  `1437 passed, 20 skipped, 1 failed`. The same demo-library failure reproduced
  on untouched commit `15083e7`; no unrelated matching behavior was changed.
- Deployed commit `d972489` to the staging branch only and enabled the authorized
  single-slot cloud Mask fallback on the Render test service.
- Re-ran the exact 60-row workbook against the paid staging providers. Six of
  six free samples completed as real Hunyuan plus cloud-Mask deterministic
  composites and preserved the selected background SHA-256. Elapsed time ranged
  from 51.23 seconds to 179.79 seconds per image.
- Visual inspection rejected all six outputs. The approved `mixed_rice`
  `style-5` background is not commercially usable, and the foregrounds contain
  cyan/teal plate, tray, cup, or edge contamination. This is recorded as a
  visual failure even though API and SHA checks passed.
- Current action is a minimal Chroma prompt/cache/gate correction followed by a
  one-sample paid visual canary. The existing 240 catalog assets remain a
  technical checkpoint, not a newly claimed visual approval.
- Implemented compiler v3 and Chroma prompt v7. Chroma generation now forbids
  cyan/teal vessels, and the exact-background path rejects boundary-connected
  cyan retained by either the local or cloud Mask.
- Verification: focused prompt/Chroma/selected-background tests passed `52`;
  full regression passed `1439`, skipped `20`, and retained only the known
  baseline demo-library failure. Python compilation and diff checks passed.
- Started Background Library V2 as an isolated research/generation track.
- Verified the taxonomy source contains exactly 40 leaf categories and created
  `background_research_v2/categories.json` plus a 240-row `catalog.csv`.
- Fixed the output contract at 1200x900 PNG, 4:3, with six visibly
  differentiated styles per category and no food, text, logo, watermark, or
  identifiable brand element.
- Existing approved catalog assets and production/runtime configuration were
  not modified. The first gated category is `topped_rice`.
- Generated the six `topped_rice` V2 sample backgrounds with the built-in
  image-generation path. One initial cream draft was rejected for dark damaged
  edges and regenerated before delivery.
- Saved six exact 1200x900 opaque RGB PNGs, six style-note folders, a labeled
  contact sheet, manifest, QA report, and UTF-8 ZIP under
  `background_research_v2/sample_delivery/`.
- Verification: six PNGs present; all dimensions 1200x900; all opaque; all six
  SHA-256 values unique; visual inspection found no food, tableware, text,
  logo, watermark, broken frame, or picture-in-picture. ZIP integrity passed,
  contains six PNGs, and hashes to
  `25cb7c601b8c56566e608e06690a3812261eee486ab62fd6c349add6fb0882bb`.
- The remaining 39 categories are paused at the explicit user-acceptance gate.
- Added `generation_plan.json` with 40 categories, 240 unique output paths,
  six category-aware style prompts per category, and eight post-sample batches.
  Validation passed for category count, slot count, unique output paths, batch
  total, and acceptance-gate status.
- Added `tools/build_contact_sheet_and_qa.py`; compilation passed and the tool
  returned `automatedPass=true` for the six-image sample. The regenerated
  UTF-8 ZIP now contains 17 files including the JSON QA evidence and six PNGs.
- Correctly disabled the Chroma fast path on the Render staging service and
  verified the live runtime setting instead of relying on the dashboard save
  animation.
- Re-uploaded the exact 60-row mixed-rice workbook. Six paid free-sample calls
  completed in 54-57 seconds each through reference-conditioned Hunyuan,
  cloud Mask, and deterministic composition. All six preserved the selected
  background identity and removed cyan-screen contamination.
- Visual QA still rejected the selected v11 background: it is a dull brown
  cyclorama/stage scene. The dish pipeline is functional; the old background
  library remains the visual blocker.
- Added a source-linked 40-category benchmark report. Official brand pages and
  Meituan rules are used only to extract high-level light, material, palette,
  and composition traits. No competitor image was downloaded into the catalog,
  and no unverified monthly-sales number was recorded.
- Added candidate background prompt/profile v13 with 40 category-specific art
  directions, six differentiated slots, and category-aware camera/placement
  contracts. Runtime activation is explicit and fail-closed; defaults remain
  unchanged.
- Verification: `49 passed` focused; `1444 passed, 20 skipped, 1 failed` full
  regression with Redis loaded only from `/tmp`. The sole failure is the known
  baseline demo-library self-check. The v13 builder dry-run planned exactly 240
  assets and made no provider call.
- Ran a real paid v13 background pilot on Render staging. Slots 1 and 2 were
  low-information colored sweeps, slot 3 was a plain stone texture, and slot 4
  generated a bowl of fried rice despite the empty-background instruction.
- Root causes: the v3 TokenHub protocol omits the Lite-only negative prompt, the
  positive prompt named the food category, the technical quality gate had no
  semantic visual approval, and new output could inherit an old sidecar's v11
  prompt version before metadata rewrite.
- Added isolated prompt/profile v14, deterministic background seeds with
  `Revise=0`, explicit generated-version snapshot binding, full 40x6 builder
  support, and regression coverage. Focused verification: `123 passed`.
- Full regression: `1450 passed, 20 skipped, 1 failed`; only the known baseline
  demo-library self-check failed. Python compilation, diff checking, and the
  no-call v14 240-asset builder plan passed.
- Corrected the local compatibility repository so generated category
  backgrounds are `pending`, not automatically approved. Focused verification
  passed `133`; full regression then passed `1452`, skipped `20`, and retained
  only the known baseline demo-library self-check failure.
- Independent review found five deployment blockers in the initial v14
  candidate: provider-visible food semantics, a product-replacement escape,
  builder prompt/pipeline mismatch, overstated seed/revision evidence, and
  v11-v13 provider-contract drift. All five were corrected before deployment.
- Revised static enumeration: 240 total prompts, 240 unique prompts, maximum
  543 characters, and zero food-lexeme hits. Focused verification passed `136`;
  full regression passed `1455`, skipped `20`, and retained only the known
  baseline demo-library self-check failure.
- Independent re-review found that v14 could still fall back to a provider that
  discarded Seed/Revise, that an offline result download failure could repeat a
  paid generation, and that an explicit v14 snapshot could inherit a v11 asset
  ID. Added exact TokenHub v3 preflight/postcondition checks, removed the v14
  Lite-only negative field, separated submission from download retry, and
  version-gated sidecar asset-ID inheritance.
- Focused background/provider/snapshot regression passed `142 passed` in 4.08s.
- Post-correction full regression passed `1461`, skipped `20` live
  infrastructure gates, and retained only the previously established baseline
  demo-library self-check failure. Python compilation and `git diff --check`
  passed. Removed the one untracked demo upload generated by the test run.
- Final review found no P1 and two P2: legacy offline prompts still received
  v14-only fields, and same-version stale sidecars were not image-hash bound.
  Limited Seed/Revise to v14 and required current image SHA-256 before an asset
  ID can be inherited. Focused regression passed `144 passed`.
- Updated the selective slot-regeneration test to declare the v14 contract it
  actually validates; the affected review/builder/runtime subset passed `102`.
- Final post-P2 full regression passed `1463`, skipped `20`, and retained only
  the known baseline demo-library failure. Compilation and diff checks passed;
  generated test uploads were removed.
- Final review reproduced a legacy v14 bypass in local/remote reuse and
  approval. Added a shared provider-evidence gate to reuse, upload, registration,
  manifest, and approval boundaries. Focused regression passed `105 passed`.
- Post-evidence-gate full regression passed `1466`, skipped `20`, and retained
  only the known baseline demo-library failure. Compilation and diff checks
  passed; generated test uploads were removed.
- Evidence-gate re-review found no P1 and one P2 in numeric coercion. Seed
  evidence now requires strict integer JSON values and rejects bool, string,
  and float inputs. Focused verification passed `110 passed`.
- Strict-seed full regression passed `1471`, skipped `20`, and retained only
  the known baseline demo-library failure. Generated test uploads were removed.
- A second runtime review found that COS/PostgreSQL approved assets and Web
  sidecars did not share the builder gate. Moved validation to shared runtime
  code, enforced it across COS/materialization/cache, and made v14 PostgreSQL
  reads fail closed because the current schema cannot store the evidence.
  Focused verification passed `128 passed`.
- Shared-runtime-gate full regression passed `1475`, skipped `20`, and retained
  only the known baseline demo-library failure. Generated test uploads were
  removed.
- Runtime re-review found no P1 and one P2 in first-response validation. Moved
  the complete evidence check before image download and display; focused
  verification passed `129 passed`.
- First-response-gate full regression passed `1476`, skipped `20`, and retained
  only the known baseline demo-library failure. Generated test uploads were
  removed.
- Closed the final v14 selected-background and Worker-contract bypass. V14
  selection now requires current prompt/category/style identity, an exact
  output-byte hash, and canonical TokenHub v3 evidence bound to that image hash.
- Froze and revalidated the evidence in both menu-batch and revision contracts;
  fresh generation now supplies current provider metadata directly instead of
  reading a stale sidecar before it is updated.
- Verification for selected backgrounds, batch contracts, and refinement
  contracts passed `130 passed`.
- Added a regression proving that a historical v14 refinement parent without
  generation evidence is rejected after its object bytes are verified.
- Combined background runtime, generation API, queue, selected-background, and
  refinement verification passed `292 passed`; Python compilation also passed.
- Full regression passed `1489`, skipped `20` live PostgreSQL/Redis tests, and
  retained only the known baseline demo-library self-check failure. Removed the
  generated `data/library/uploaded_*` test artifact after the run.
- Narrowed revision-contract changes to v14 only so legacy v11/v12 requests keep
  their prior canonical shape and idempotency digest input. Focused revision,
  queue, settlement, selected-background, and batch verification passed `206`.
- The post-compatibility full regression again passed `1489`, skipped `20`, and
  retained only the established baseline demo-library failure. Removed the new
  generated upload artifact after the run.
- Moved v14 generation-evidence validation ahead of selected-background COS
  upload, Worker object download, and refinement object reads. Focused tests
  confirm invalid evidence causes no storage-body I/O; `146 passed`.
- Final full regression after the fail-before-I/O correction passed `1490`,
  skipped `20`, and retained only the known baseline demo-library failure.
  Removed the generated upload artifact.
- Independent review found that an offline v14 provider response with a valid
  action but invalid Seed evidence could still be downloaded and reported as
  PASS. Moved the complete shared evidence check before image download.
- Added absent/string/float/bool/mismatched Seed regressions. They confirm no
  download and no repeated paid submit; affected verification passed `242`.
- The post-builder-gate full regression passed `1495`, skipped `20`, and
  retained only the established baseline demo-library failure. Removed the
  generated upload artifact.
- Independent review found the Web v14 path could substitute the requested Seed
  when the provider returned a falsy Seed. V14 now validates only the raw
  provider value and rejects None, zero, bool, string, float, and mismatch
  before download. Affected Web/builder/runtime/contract tests passed `242`.
- The raw-Seed post-fix full regression passed `1495`, skipped `20`, and kept
  only the established baseline demo-library failure. Removed the generated
  upload artifact.
- A second reviewer found Revision Worker lacked an independent v14 gate and
  provider/model identity was default-filled in builder/Web metadata. Revision
  Worker now validates before object reads or Gemini; v14 Web/builder paths use
  only raw provider identity fields.
- Valid v14 refinement and invalid missing/string-Seed contracts with recomputed
  request digests are covered. Invalid contracts perform no body reads or
  provider calls; affected verification passed `279`.
- The Revision Worker/provider-identity post-fix full regression passed `1501`,
  skipped `20`, and retained only the established baseline demo-library
  failure. Removed the generated upload artifact.
- Added HMAC attestation to newly created revision contracts using the existing
  object-signing secret. Web now rejects a missing secret before replay/debit
  and rejects a tampered persisted contract; Worker rejects unsigned or
  modified contracts before object reads or provider calls.
- Added positive legacy compatibility and negative downgrade/missing-secret/
  tampered-replay coverage. Revision, queue, settlement, PostgreSQL, and
  compilation verification passed `150 passed`.
- Added a separate HMAC attestation to authenticated formal menu-batch
  contracts. The Web signs before debit/enqueue; Product Worker verifies before
  app import, object storage, or generation. Recomputed-SHA scene downgrade,
  arbitrary contract tampering, and missing-secret paths now fail closed while
  newly signed legacy scenes remain compatible.
- Verification passed `37` batch/Worker tests, `224` broader generation/API/
  security tests, and `69` Redis-dependent product API tests using the existing
  `/tmp/waimai-bgfix-deps` dependency path.
- The first post-attestation full suite passed `1509`, skipped `20`, retained
  the established demo-library baseline failure, and found one deterministic
  E2E failure caused by the new missing-secret gate.
- The E2E acceptance context now derives a test-only 64-character signing key
  from its temporary root and restores the original environment after use.
  Acceptance verification passed `4 passed`; generated upload fixtures were
  removed before the final full run.
- Final post-attestation full regression passed `1510`, skipped `20` live
  infrastructure gates, and retained only the previously reproduced baseline
  demo-library self-check failure. Python compilation and `git diff --check`
  passed; the final generated upload fixture was removed.
- Independent review then demonstrated that recomputing the public request
  digest could still route a forged same-user debit through Web/SQLite refund,
  and that changing only the attestation version allowed cross-domain replay.
- Added a shared domain-separated HMAC format binding application context,
  menu/revision job domain, version, and canonical contract. Added fail-closed
  attestation checks to persisted PostgreSQL completion/settlement and direct
  Redis completion parsing before manifest reads.
- Added regressions for menu-to-revision signature replay, no object read after
  contract tampering, and no PostgreSQL claim/refund after a debit-order
  mutation. Initial contract/settlement verification passed `113 passed`.
- A separate reviewer and Tencent's official TokenHub examples confirmed that
  successful Hunyuan 3.0 submit/query responses return job/request IDs, status,
  URL, and optional revised prompt, but do not document a returned Seed.
- Replaced the impossible v14 provider-Seed requirement with explicit evidence
  for the exact Seed/Revise controls submitted by the TokenHub adapter. The
  persisted evidence marks `submitted-request`, whether Seed was echoed, and
  keeps echoed/applied claims false when the provider omits them.
- Added a regression using the documented asynchronous response shape without
  Seed and retained fail-before-download tests for malformed echoes, missing
  submitted controls, provider identity drift, and non-v3 actions. Affected
  background/runtime/contract tests passed `255 passed`.
- Complete post-correction regression passed `1516`, skipped `20` live
  PostgreSQL/Redis infrastructure tests, and retained only the previously
  reproduced baseline demo-library self-check failure. Compilation, diff
  checking, and staged gitleaks scanning passed; generated upload fixtures were
  removed.
- Independent review found one remaining TokenHub boundary bug: the response
  normalizer used boolean fallback, so an explicit `seed: 0` or `seed: false`
  became indistinguishable from an omitted Seed and could reach image download.
- Preserved raw Seed key presence in the normalizer and added an asynchronous
  submit/query regression for both falsy values. Both are rejected with zero
  download side effects. Python compilation and the focused background/Web
  suite passed `100 passed`.
- The first full run showed the known demo-library baseline failure plus two
  missing-`redis` import failures because the default interpreter did not load
  the isolated dependency directory. `redis==5.0.8` is already declared by the
  repository and was present under `/tmp/waimai-bgfix-deps`.
- Re-running with that isolated path passed `1517`, skipped `20` live
  infrastructure tests, and retained only the previously reproduced baseline
  demo-library self-check failure. Removed the generated upload fixture.
- Independent review reproduced two HMAC ordering gaps: PostgreSQL outbox could
  enqueue and publish a tampered contract, and the local batch runner could
  write `running`/`failed` before rejecting the same contract.
- Added domain-specific attestation validation to outbox preparation and moved
  local-runner attestation validation before the exception/compensation region.
  Tampered public-digest regressions prove zero Redis enqueue, outbox publish,
  status update, refund, and generation execution. Focused verification passes
  `33 passed`; Python compilation passes.
- Broader product queue/API/PostgreSQL-gated/settlement/revision/background
  verification passed `331 passed`.
- Complete post-fix regression passed `1520`, skipped `20` live infrastructure
  tests, and retained only the established demo-library baseline failure.
  Generated upload fixtures were removed; independent exact-SHA review remains.
- Independent review reproduced one more TokenHub response ambiguity: explicit
  `seed: null` was accepted as though the provider omitted the Seed field.
- Added raw provider-Seed presence evidence across normalization, Web generation,
  shared evidence validation, and the offline 240-background builder. Explicit
  null/zero/boolean/type drift/mismatch now fails before download while the
  official omitted-Seed response remains valid. Focused verification passes
  `101 passed`; Python compilation passes.
- Complete regression after the Seed-presence correction passed `1521`, skipped
  `20` live infrastructure tests, and retained only the established baseline
  demo-library failure. Generated upload fixtures were removed.
- Independent review reproduced conflicting TokenHub `seed`/`Seed` aliases: a
  correct lowercase echo plus null or mismatched uppercase echo reached download.
- TokenHub normalization now accepts dual aliases only when both are strict,
  equal integers. New asynchronous regressions prove both conflict forms fail
  with zero download, while an equal pair normalizes successfully. Focused
  verification passes `103 passed`; Python compilation passes.
- Complete dual-alias post-fix regression passed `1523`, skipped `20` live
  infrastructure tests, and retained only the established demo-library baseline
  failure. Generated upload fixtures were removed.
- Independent review found pre-presence v14 submitted-request sidecars could
  still be reused because a missing `providerSeedPresent` was treated like
  explicit false. Removed that compatibility before v14 deployment.
- Submitted-request omission now requires explicit false and an exact echo
  requires explicit true. Approved catalog records retain the presence field;
  missing-field sidecars are rejected. Related background/selection/menu/
  revision contract verification passes `241 passed`.
- Complete strict-presence post-fix regression passed `1525`, skipped `20` live
  infrastructure tests, and retained only the established demo-library baseline
  failure. Generated upload fixtures were removed.
- Two independent reviewers returned PASS for staged SHA-256
  `8248b2c1724ea3c3bc70e5c228b20c7e5dfa0023318d40176637191b4927c2b8`.
  They independently checked old-sidecar rejection, menu/revision HMAC upgrade,
  Seed omission/exact echo/malformed aliases, outbox and local side-effect gates,
  and catalog-to-runtime presence preservation. No P0/P1/P2 remains in scope.
- Deployed commit `985c6c9` remained healthy on the protected Render test
  service. Readiness showed Hunyuan TokenHub `hy-image-v3.0` and private COS
  ready; Redis/PostgreSQL/independent Workers, Gemini, SMS, and production auth
  remain separate blockers.
- Uploaded the exact 60-row mixed-rice workbook (SHA-256
  `ca4093931d503c08eff9fc9c959214c9b4ce0817d980cbbfe9240325af857df3`).
  Store-level classification returned `mixed_rice`, confidence 96.
- The first real background completed in 26.69 seconds, but provider metadata
  proved the service was still using `style-background.v13`. Manual review
  rejected the 1024x768 result as an unusable smooth brown gradient.
- One paid free-sample request completed Hunyuan v3 foreground generation,
  Tencent cloud Mask extraction, deterministic lossless composition, and
  private COS persistence. Background identity and persisted outside-mask
  pixels were verified. The result proves the chain is working, not that its
  aesthetics or combo semantics are accepted.
- Reworked the not-yet-activated v14 prompts into 240 unique commercial scene
  directions: two textured studio slots and four tabletop slots with bounded
  category-specific edge accents. Added a background-only texture/detail gate;
  the rejected live image now fails as `low_information_gradient`.
- Relevant background, Hunyuan, catalog-builder, selected-background, and image
  pipeline tests pass `147 passed`; Python compilation and diff checking pass.
- The offline 240-background builder was still using the generic image gate
  and continued after a failed paid asset. It now uses the background-specific
  gate, defaults to one provider attempt, reserves an explicit run-wide paid
  call budget, and stops on the first failure unless continuation is explicitly
  requested. Regression tests prove the second asset is not submitted after a
  first-asset failure or after the paid budget is exhausted; the focused suite
  now passes `149 passed`.
- Independent read-only menu audit found 18 unknown rows, optional hot-dog and
  drink choices contaminating row categories, collapsed combo quantities, and
  a meal combo incorrectly choosing a drink container. These remain the next
  isolated correction before formal menu acceptance.
- Corrected the authorized Render staging variable
  `MIXED_RICE_BACKGROUND_PROMPT_VERSION` from v13 to v14. Manual deploy
  `dep-d9uhqj15efls73cmonc0` reached Live on commit `1852a3d`; production was
  not touched.
- Re-uploaded the exact 60-row workbook and generated a real paid v14 style-1
  background. Hunyuan 3.0 returned in 14.17 seconds with canonical v14
  evidence; the downloaded 1024x768 asset SHA-256 is
  `5dcaee52998f961d81c4268781cc73e20cc82dbec0327be3876defe528e84984`.
  Manual review confirms a textured stone studio set instead of the rejected
  color block, but it is not yet a customer-approved aesthetic.
- Generated row 2 as a real free sample against that exact background. The
  57.75-second Hunyuan foreground, Tencent Mask, deterministic composite, and
  private COS chain passed background identity and produced full-frame
  1024x768 SHA-256
  `c0d3b7940c1001d92208022c61a2e8a8e1fd6187fd76c3c0aeb4879b677053cf`.
  No inner frame or floating plate was observed; menu semantics and broader
  visual quality remain pending.
- Generated a second paid v14 background for style-2 in 18.29 seconds. Its
  canonical Hunyuan 3.0 SHA-256 is
  `ea828e5b2b66d4bad08c53f0836839336f1dbd4a49d7776affef1074dac104be`,
  but manual review rejected it because it repeated the central raised stone
  slab and only changed the scene to near-monochrome orange-red. Further paid
  calls were stopped.
- Reworked the v14 pilot prompt again: the first instruction now requires one
  flat tabletop and forbids plinths, risers, boards, trays, and center objects;
  the six slots now use different light, camera, material, and edge-accent
  families instead of two color-swapped studio sets. Style-2 now uses a dark
  textured surface with only a weak category-color edge reflection.
- Updated stale background test doubles to call the background-specific quality
  gate and opted the six-asset checkpoint test into an explicit six-call paid
  budget. The affected suite passes `157 passed`; the full run currently
  reports `1527 passed, 20 skipped` plus the known demo-library failure and two
  menu-semantic assertions that are being fixed in the isolated menu step.
- Commit `77fe4f3` reached Render staging Live in auto-deploy
  `dep-d9ui593l550s73dde0s0`. The paid style-2 recheck completed in 25.50
  seconds with Hunyuan 3.0/v14 SHA-256
  `b1c56869488ab95f9550d93dc3c4e70593a79b60fa56ee5851e0ae48a6d39626`.
  It removed the orange monochrome treatment but exposed the table's front
  edge, thickness, legs, and under-table space, so visual review rejected it
  and further paid calls stopped.
- Tightened the next local prompt to a close-cropped tabletop extending beyond
  all four frame edges, with explicit first-line bans on walls, horizons,
  table edges, thickness, legs, and under-table space. The affected suite
  remains `157 passed`; deployment and paid recheck remain pending.
- Independent review found a P0 contract mismatch: v14 background prompts used
  new table scenes while foreground and catalog metadata still inherited v13
  pure-color definitions. Added one structured six-style source for v14 prompt,
  foreground scene contract, and builder metadata; v11/v13 remain unchanged.
  Six structural fingerprints are unique by surface, camera, lens, light, and
  accent zone. Background and menu-semantic focused verification passes
  `321 passed`.
- Integrated and independently reviewed the menu semantics correction. The
  exact workbook still has 60 rows and 26 combos; `burger_hotdog` contamination
  is now zero, unknown rows are 4 (one greeting and three egg add-ons), and the
  ghost component `品` is zero. Combos cannot choose a drink cup container, and
  black-pepper grilled cutlet is explicitly rendered as fully cooked boneless
  pork cutlet.
- Dependency-complete full regression now passes `1535`, skips 20 live
  infrastructure tests, and retains only the previously reproduced baseline
  demo-library self-check failure. Generated test upload fixtures were removed.
- Closed the remaining v14 catalog-metadata mismatch without changing v11-v13.
  Added one prompt-version-aware slot metadata helper and routed runtime
  sidecar metadata, COS and PostgreSQL catalog reads, catalog API payloads,
  offline builder records, and review-sheet labels through it. Added explicit
  v14 and frozen-v13 regressions; the focused background/catalog/selection/menu
  and Prompt Compiler suite passes `159 passed`. Python compilation and
  `git diff --check` pass. No provider call, production change, database
  migration, commit, push, or deployment occurred in this step.
- Complete post-metadata regression reports `1537 passed, 20 skipped, 1 known
  baseline failure` in 22.44 seconds. The only failure is
  `MatchingEngineDemoLibraryTest.test_demo_library_self_check`, already
  reproduced on the untouched baseline; no new v14, catalog, parser, compiler,
  or composition failure appeared. The generated upload fixture was identified
  for cleanup before staging.
- Exact-workbook prompt inspection caught a structured-choice leak before
  deployment: a combo that selected `热狗肠` and rejected `随机饮品` still
  inherited `饮品只放一杯` from its raw name. The compiler now rebuilds the
  provider-visible dish name from required plus selected components and emits
  a cup requirement only when the resolved components include a drink. Tests
  cover both the rejected-drink and selected-drink paths; the related suite
  passes `115 passed`. No paid request or environment change occurred.
- Re-audited all 26 combos in the exact 60-row workbook. No resolved prompt
  asks for a drink unless the selected component is a drink. Row 2 now resolves
  to `蜜汁烤肉拌饭+黑椒烤排+热狗肠套餐`, with `煎蛋` and `随机饮品` absent.
  Full regression reports `1538 passed, 20 skipped, 1 known baseline failure`;
  Python compilation and `git diff --check` pass.
- Expanded deterministic acceptance to compile all 60 real workbook rows
  against all six v14 scene contracts. The first run found 18 failures from
  three repeated rows because rejected option `猪排` was a substring of the
  selected `烤排` description `无骨猪排`. Changed that explicit meat
  description to `无骨猪肉排`; it still specifies pork but no longer denotes
  the rejected option. The 60 x 6 matrix now passes `360/360`; the affected
  parser/compiler/selected-background suite passes `79 passed`.
- Final full regression after the complete 360-prompt correction reports
  `1539 passed, 20 skipped, 1 known baseline failure` in 23.06 seconds. The
  only failure remains the untouched-baseline demo-library self-check. Removed
  all generated upload fixtures; independent review remains before commit.

## Step 104 - explicit v14 review and drink-role semantics
- Added explicit prompt-version plumbing to background manifest lookup,
  reusable-entry verification, review-sheet generation, and the offline review
  CLI. A v14 manifest can now be approved while the builder global remains on
  its legacy default.
- Added a structured `role=drink` semantic rule so brand-only selections such
  as `王老吉` still request exactly one unbranded cup, while ordinary side
  selections do not.
- Verification: focused compiler/catalog/runtime/selection tests passed
  `122 passed`; Python compilation and `git diff --check` passed.
- No provider request, COS mutation, Render deployment, commit, or push was
  performed in this step.
- Re-ran the exact real workbook across all six v14 scene contracts: 60 rows,
  `mixed_rice` confidence 96, `360/360` successful compilations, zero rejected
  choice leaks.
- Full regression reports `1543 passed, 20 skipped, 1 known baseline failure`
  in 22.20 seconds. The only failure remains
  `MatchingEngineDemoLibraryTest.test_demo_library_self_check`, previously
  reproduced on untouched commit `77fe4f3`. The generated test upload fixture
  was removed.
- Independent staged-diff review reports P0 PASS and P1 PASS. It also ran a
  cross-version probe with builder global v13 and explicit review version v14;
  manifest and review-sheet writes remained under v14.
- Closed the reviewer's P2 test-evidence observation by forcing that mismatched
  global/explicit version in the repository approval test and by invoking the
  review CLI `main` with v14, asserting v14 reaches the approval call. The
  strengthened review/compiler suite passes `28 passed`.
