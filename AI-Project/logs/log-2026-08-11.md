# Execution Log

## 2026-08-11

- Resumed the product-completion goal in an isolated clone; the original
  worktree was left untouched because it contains unrelated local changes.
- Fixed the audit baseline at staging commit `39e6fdf` and confirmed Render
  currently deploys only one free Web Service. Redis, PostgreSQL, Product
  Worker, SMS, payment, and KYC production services are not deployed.
- Confirmed the test service has TokenHub/Tencent/COS variable names but no
  Gemini API key. No secret value was written to source control or memory.
- Added sanitized `/api/image-providers` routing: Hunyuan handles backgrounds,
  free samples, and formal generation; Gemini handles single-image refinement.
- Added customer-side Gemini availability gating and separate Hunyuan/Gemini
  readiness cards in the admin console.
- Hardened Gemini image refinement by requiring the official Google
  Interactions endpoint and sending `store=false`; permanent provider errors
  now fail without Worker retries.
- Corrected the Render Product Worker blueprint to receive `DATABASE_URL` and
  changed the customer Web health check to the explicit public `/healthz`
  liveness endpoint.
- Focused tests passed: `132 passed`.
- Full regression passed after the blueprint/liveness fix: `1366 passed, 20 skipped`; skips are real
  PostgreSQL/Redis integration gates, not production evidence.
- Render staging Basic Auth rotation was initiated so the live site can be
  tested without retrieving the prior password. Deployment verification and
  real-menu rerun remain in progress.
- Pushed `dd84051` to `codex/gemini-dual-provider-staging` and switched only
  the protected Render test service to that branch. The deploy succeeded and
  `/healthz` returned HTTP 200.
- Live provider checks confirmed Hunyuan 3.0 and COS are ready. Gemini is
  intentionally unavailable because `GEMINI_API_KEY` is absent; the customer
  UI now blocks that paid refinement path instead of pretending it is usable.
- Ran the exact 60-row mixed-rice workbook remotely. Upload, taxonomy, all six
  approved COS backgrounds, SHA verification, and selected-background binding
  passed. The first free sample failed after 75.961 seconds with a provider
  result-image TLS handshake timeout.
- Added a bounded retry only around the idempotent provider result-image GET.
  The paid generation submit is never retried, so the fix cannot duplicate a
  paid image request. Focused security/generation tests passed: `83 passed`.
- Pushed result-image retry commit `42a3739`; Render deployed it successfully.
- Re-ran the exact 60-row workbook. Upload, `mixed_rice` classification, all
  six approved COS backgrounds, selected `style-3`, and six paid Hunyuan free
  samples passed in 330.054 seconds. All six outputs preserved the selected
  background identity and output SHA verification.
- Visual QA rejected samples five and six even though structural checks passed:
  Chinese combo `烤排` became a western steak and the rice/choice semantics
  were wrong. This is a semantic-generation failure, not a provider, balance,
  COS, or selected-background failure.
- Canceled formal job `generation-956c3ed9aed687dffa4365d6` at 14/60 before
  accepting more wrong images. The test ledger charged 600 points, refunded
  600 points, netted zero, and restored the account balance to 1880.
- Root cause: `split_components` split inside square brackets before removing
  the bracket delimiters, dropping `烤肉` and leaving malformed `鸡排】`; the
  chroma prompt was also truncated at 250 characters and lacked Chinese
  mixed-rice/cutlet/choice semantics.
- Added a narrow bracket-component parser correction and prompt contract v2.
  It requires rice for rice categories only, disambiguates `烤排`/`鸡排`/
  `腿排`/`猪排`, enforces combo counts and one-of choices, forbids inset frames,
  and keeps the prompt below 600 characters. Non-rice categories have a
  regression guard. Focused verification passed: `48 passed`.
- Independent review rejected the earlier result-download retry as
  production-safe because a provider-controlled URL could target arbitrary
  hosts and the legacy replacement fallback could invoke a second paid model
  call after download failure.
- Restricted provider image downloads to credential-free HTTPS under the
  Tencent result-domain allowlist, rejected any non-public DNS answer, and
  revalidated every redirect. Retries now share a bounded wall-clock deadline.
- Added `ProviderResultDownloadError`; this failure is recorded but never
  enters the legacy paid text-to-image fallback. Focused security, generation,
  parser, and selected-background tests passed: `104 passed`.
- Complete regression after semantic and security fixes passed: `1374 passed,
  20 skipped`. The skips still require live PostgreSQL/Redis infrastructure.
- Second independent security review reproduced two remaining duplicate-cost
  paths: uppercase/malformed result URLs could escape the download sentinel,
  and an unknown TokenHub submit outcome automatically invoked the legacy
  cloud provider.
- All result decoding, downloading, and file-write failures now become
  `ProviderResultDownloadError`, including uppercase and malformed URL forms.
  The configured TokenHub route no longer falls back to another paid provider
  after any ambiguous outcome; its version token is
  `tokenhub-fail-closed-v2`.
- Focused fail-closed verification passed `115 passed`; final full regression
  passed `1375 passed, 20 skipped`.
- Final P1 review found directory creation and cleanup errors outside the
  paid-result sentinel. `save_result_image` now aggregates failures across
  directory creation, decode/download, save/replace, image close, and temporary
  cleanup; cleanup can neither overwrite the primary error nor trigger a paid
  fallback. Focused verification passed `116 passed`; full regression passed
  `1376 passed, 20 skipped`.
- Independent final P1 review passed for the default TokenHub staging route.
  Remaining P2 items are DNS-to-connection binding, strict resolver/body
  deadline enforcement, and durable provider-result recovery after all three
  GET attempts fail.
- Deployed semantic prompt v2 commit `6786792` and reran the exact workbook.
  All six real Hunyuan samples passed output size, hash, and selected-background
  identity checks in 399.733 seconds. Visual QA passed four and rejected two:
  the row-13 `三选一` sample showed both an egg and a drink, and the row-61
  sample added Chinese text to the meal box. Formal generation was not started.
- Added prompt contract v3 as a minimal local candidate. Explicit slash/or
  choice groups now resolve to one deterministic option and rejected options
  are removed from combo components. Hunyuan v3 receives `Revise=0` and a
  stable 32-bit product/category/quality seed. The prompt starts with a strict
  no-readable-text and plain-container rule, and prompt versions invalidate
  the two rejected cached foregrounds.
- Independent review found the first choice fix was Chroma-only and that its
  regex misparsed `或者`, ASCII words containing `or`, and comma/bar choice
  lists. The shared resolver now covers both Chroma and reference-conditioned
  prompts, uses the explicit option count to discard prefixes, and supports
  slash, bounded `or`, `或者`, commas, enumeration commas, and vertical bars.
  Seed metadata now distinguishes requested from actually applied seeds on
  TokenHub Lite and legacy cloud paths.
- Focused prompt/provider/contract verification passed `154 passed`. A real
  60-row prompt audit passed all 21 explicit-choice rows on both foreground
  routes without leaking rejected options. The full local suite passed `1382
  passed, 20 skipped`; skipped tests require live PostgreSQL/Redis, and the
  only warning is the pre-existing LibreSSL warning.
- Final independent re-review returned PASS after `seedApplied` became true
  only for TokenHub v3, while Lite/cloud response seeds remain recorded solely
  as provider output metadata.
- Render deploy `dep-d9tq6i8ae00c73bjhnv0` made `50a6e1f` live. A fresh real
  six-sample run passed all structural size/hash/background checks. Visual QA
  verified the two targeted fixes: the explicit `三选一` row contains only an
  egg, and the self-select meal container contains no text. Samples 1, 2, and
  5 were still rejected because `烤排` appeared as a pink western steak.
- Prompt v4 removes every occurrence of the unwanted steak noun from the
  positive prompt and specifies only fully cooked light-brown Chinese
  black-pepper boneless pork cut into six slices. Fully cooked positive-only
  descriptions replace the old negations for chicken, thigh, and pork cutlets.
  Prompt/cache versions were bumped; formal 60-row generation remains stopped.
- Audited all six active `mixed_rice` backgrounds at full size. Visual result:
  0/6 accepted. The repeated low-angle empty-table design, visible table edges,
  missing support-plane metadata, and fixed bottom-aligned compositor explain
  the upright and pasted-dish appearance.
- Added `prompt_compiler.py` as the deterministic pre-generation stage. It
  resolves explicit choices, removes rejected alternatives, preserves the
  positive-only cooked-meat semantics, binds dish generation to structured
  camera/light/placement contracts, fits only complete prompt segments, runs
  Prompt QA, and emits stable prompt/payload/scene hashes and Seed. Python
  compilation passed; no provider call or catalog mutation occurred.
- Wired `product-image-compiler.v1` into reference-conditioned and Chroma
  generation. Exact-background pipeline v5 now records and verifies compiler,
  prompt, payload, and scene-contract digests before reusing a foreground.
- Added batch-contract schema v3 so the selected background's asset-bound
  Scene Contract is frozen into the queued Worker contract and validated on
  materialization.
- Replaced fixed bottom-aligned placement with compiled anchors and dimensions;
  added a subtle directional contact shadow whose pixels are included in the
  background-identity modification mask.
- Added dedicated Prompt Compiler and compositor tests. Focused verification:
  `58 passed, 4 subtests passed`; broader affected-suite verification also
  passed `81 passed, 4 subtests passed`. No Hunyuan call occurred.
- Audited the formal generation throughput path. Root causes were the
  process-wide TokenHub mutex, `FINAL_GENERATION_WORKERS=3`, and the six-call
  formal-batch cap. Added `provider_capacity.py` with a ten-worker/ten-slot,
  100-image/3600-second contract and observed throughput metadata.
- Added Redis-leased TokenHub slots for account-level cross-process capacity;
  lease cleanup failures cannot replace a paid provider success. The same gate
  is used by the Web/Product path and Prompt Worker.
- Added Tencent's current official synchronous `hy-image-v3` TokenHub adapter
  side by side with the legacy adapter. No paid request was made.
- Added Gemini source-aspect output requests and safe same-ratio canvas
  normalization. Revision contract schema v2 freezes the Gemini provider and
  output snapshot into the request digest before billing.
- Added a dedicated `product-revision` Redis queue and a ten-consumer Revision
  Worker blueprint. Python compilation passed for all changed runtime modules;
  regression tests are pending.
- Corrected the capacity gate so requested concurrency ten does not become an
  unverified paid-provider claim. Runtime active concurrency is
  `min(requested, verified-or-one)`; both local and Redis-distributed gates use
  that value.
- Added a pre-debit formal-batch capacity check. A menu with more rows than
  `TENCENT_HUNYUAN_SYNC_LIMIT` returns
  `generation_batch_capacity_insufficient` before queue lookup or point debit.
- Added the current `hy-image-v3` synchronous adapter contract, cross-process
  Redis slot tests, a 100-row scheduler test that reaches exactly ten row
  workers, and a ten-consumer Revision Worker test.
- Added Gemini 4:3 output negotiation, exact source-canvas normalization,
  wrong-aspect fail-closed behavior, timeout validation, provider snapshot
  hashing, and independent live-worker readiness tests.
- Capacity, Gemini, refinement, Redis, Render-blueprint, and selected-background
  focused verification passed `223 passed, 16 subtests passed`. This is
  structural/simulated evidence only; no paid provider call was made and the
  one-hour production target is not yet claimed.
- Preserved the v11 background-builder compatibility contract: the default
  prompt path continues calling the established two-argument function, while
  the isolated v12 pilot passes its explicit prompt version. The previously
  failing selective-regeneration test now passes.
- Final focused verification passed `225 passed, 16 subtests passed`; full
  regression passed `1409 passed, 20 skipped, 157 subtests passed` in 23.23
  seconds. `git diff --check`, Python compilation, and `node --check` for both
  frontend bundles passed.
- Ran `scripts/menu_e2e_acceptance.py` in deterministic mode against the real
  60-row mixed-rice workbook. All eight stages passed: upload, taxonomy,
  six-background generation, selected-background binding, six free samples,
  60 formal images, manifest verification, and Meituan ZIP export. Formal
  generation took 36.251 seconds; the report is
  `/tmp/waimai-mixed-rice-e2e.json` and explicitly records that no production
  provider was verified.
- Gitleaks found no secret in the complete tracked diff. The whole-repository
  scan still reports only pre-existing test fixtures/payment placeholders;
  none is introduced by this patch. No paid provider, production service,
  database migration, or production configuration was touched.
- Hardened the 100-image capacity contract after independent review. Readiness
  now stays red until a complete real 100-image batch duration is recorded in
  addition to verified ten-slot provider entitlement and measured P95.
- Raised the Redis provider-slot lease to at least request timeout plus poll
  timeout plus 60 seconds, preventing slot reuse during the configured paid
  provider window when a lease heartbeat is lost. Capacity tests passed
  `8 passed`; no external provider call was made.
- Closed the Gemini transport redirect boundary: the production opener rejects
  redirects, the initial URL remains the exact official Interactions endpoint,
  and malformed ports return invalid readiness instead of raising.
- Routed revision outbox dispatch and settlement through the dedicated
  `product-revision` queue using the frozen PostgreSQL job type. Added
  compensation for a fresh non-PostgreSQL debit when Redis returns an
  idempotency conflict. The affected suite passed `111 passed`; no Gemini call
  or billing mutation occurred.
- Added prompt-version-bound Background Scene Contract v2. Legacy v11
  backgrounds retain their real 25-degree camera contract; isolated v12
  mixed-rice candidates retain their 52-62 degree contract. Runtime promotion
  remains blocked on six real visual approvals.
- Bumped exact-background output/library compatibility to pipeline v6 and
  required row-specific compiler plus scene digests for Tencent cache reuse.
  Forged compile digests are rejected. Focused prompt, background, compositor,
  batch-contract, app-generation, and asset-runtime tests passed
  `130 passed, 1 skipped`.
- Complete regression passed `1421 passed, 20 skipped` in 23.92 seconds after
  installing the repository-declared Redis client only under `/tmp` for the
  test process. Python compilation, JS syntax, whitespace checks, and Gitleaks
  scans of tracked and untracked changes all passed.
- Re-ran deterministic acceptance with the real 60-row mixed-rice workbook.
  All eight stages passed; formal generation took 38.866 seconds, all outputs
  matched the selected background SHA, and the Meituan archive contained 60
  images plus manifest. Evidence is `/tmp/waimai-mixed-rice-e2e-v6.json`; no
  paid provider or production environment was exercised.
- Hardened capacity acceptance so only a SHA-256-bound real 100-image evidence
  file can verify the one-hour target. Fixed the target at exactly 100 images
  and 3600 seconds, retained ambiguous Redis leases after heartbeat/release
  failures, and disabled unverified cloud Mask fallback. Focused capacity and
  acceptance verification passed.
- Fixed the background v12 version chain. API snapshots and schema-v3 batch
  contracts now preserve and validate `backgroundPromptVersion`; Worker
  restoration uses the frozen Scene Contract version instead of the dataclass
  default. COS and PostgreSQL manifests hash prompts with their requested
  version, and runtime v12 promotion is restricted to `mixed_rice`.
- Focused background, batch-contract, selected-background, capacity, and real
  menu acceptance test modules passed `96 passed` in 12.29 seconds. No paid
  provider call, catalog mutation, deployment, or production change occurred.
- Full post-hardening regression passed `1431 passed, 20 skipped` in 25.44
  seconds. Skips require live PostgreSQL/Redis; the only warning is the
  pre-existing Python 3.9 LibreSSL warning.
- Python compile-all, both frontend `node --check` commands, and
  `git diff --check` passed. Gitleaks scanned the tracked diff and all untracked
  candidate files independently and found no secret.
- Ran the exact 60-row mixed-rice workbook through all deterministic acceptance
  stages. Classification returned `mixed_rice` at 96 confidence; six unique
  backgrounds, six free samples, 60 formal images, all 60 asset SHA checks,
  selected-background identity, billing arithmetic, and a 61-entry Meituan ZIP
  passed. The manifest contains 42 unique outputs because 18 exact duplicate
  products were safely reused. Report: `/tmp/waimai-mixed-rice-e2e-final.json`.
- This acceptance used `deterministic-local`; it did not call a paid provider,
  did not prove 10 paid concurrent requests, and did not emit 100-image
  capacity evidence. The generated `data/library/uploaded_*` test directory
  was removed after verification.
- Capacity re-review found four remaining boundary defects: real acceptance
  forced one Worker, release-Lua result zero was ignored, observed performance
  used mutable targets, and cloud Mask concurrency was process-local.
- Corrected all four. Real acceptance uses ten row workers but paid calls remain
  clamped by verified provider entitlement; 100/3600 is immutable; release
  mismatch retains an ambiguous Redis slot; and staging/production cloud Mask
  requires a shared Redis gate. Focused verification passed `91 passed` in
  12.88 seconds; scoped compilation and `git diff --check` passed.
- Final post-capacity full regression passed `1435 passed, 20 skipped` in
  24.89 seconds. Skips remain live PostgreSQL/Redis gates; the only warning is
  the pre-existing Python 3.9 LibreSSL warning.
- Repeated the exact 60-row deterministic acceptance after the final capacity
  fixes. All eight stages passed again; report
  `/tmp/waimai-mixed-rice-e2e-final.json` remains explicitly non-paid. Removed
  the generated repository test directory after verification.
- Final capacity re-review found that disabling Chroma could still call cloud
  Mask directly without the fallback guard. Moved the distributed-capacity
  requirement into `tencent_extract_foreground_mask` itself, so every
  staging/production cloud-Mask path fails before the provider request unless
  verified concurrency and Redis are both present. Focused verification passed
  `92 passed` in 12.89 seconds.
- Final full regression after the direct cloud-Mask guard passed `1436 passed,
  20 skipped` in 25.53 seconds. Skips and the LibreSSL warning are unchanged.
- Final scoped Python compilation, both frontend syntax checks,
  `git diff --check`, and tracked/untracked Gitleaks scans passed. Generated
  bytecode was removed.
- Independent final capacity review returned PASS for ten-worker acceptance
  with quota clamp, release-Lua ambiguity, immutable 100/3600 thresholds, and
  distributed cloud-Mask enforcement. Its focused run passed `92 passed, 16
  subtests passed`; no new P1 was found and no paid call was made.
- Independent background-version review returned PASS for API snapshot to
  Worker v12 preservation, mixed-rice-only promotion, and COS/PostgreSQL v12
  prompt hashes. The earlier Gemini/queue review also returned PASS.
- Committed the candidate as `2236784` (`Build concurrent dual-provider image
  pipeline`) and pushed it only to
  `origin/codex/gemini-dual-provider-staging`. No push to `main`, production
  deployment, database migration, or production configuration occurred.
