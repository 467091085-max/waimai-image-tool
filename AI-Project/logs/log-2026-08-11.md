# Execution Log

## 2026-08-11

- Resumed the product-completion goal in an isolated clone; the original
  worktree was left untouched because it contains unrelated local changes.
- Fixed the audit baseline at staging commit `39e6fdf` and confirmed Render
  currently deploys only one free Web Service. Redis, PostgreSQL, Product
  Worker, SMS, payment, and KYC production services are not deployed.
- Confirmed the test service has TokenHub/Tencent/COS variable names but no
  Gemini API key. No secret value was written to source control or memory.
- Added sanitized `/api/image-providers` routing: Hunyuan handles backgrounds,
  free samples, and formal generation; Gemini handles single-image refinement.
- Added customer-side Gemini availability gating and separate Hunyuan/Gemini
  readiness cards in the admin console.
- Hardened Gemini image refinement by requiring the official Google
  Interactions endpoint and sending `store=false`; permanent provider errors
  now fail without Worker retries.
- Corrected the Render Product Worker blueprint to receive `DATABASE_URL` and
  changed the customer Web health check to the explicit public `/healthz`
  liveness endpoint.
- Focused tests passed: `132 passed`.
- Full regression passed after the blueprint/liveness fix: `1366 passed, 20 skipped`; skips are real
  PostgreSQL/Redis integration gates, not production evidence.
- Render staging Basic Auth rotation was initiated so the live site can be
  tested without retrieving the prior password. Deployment verification and
  real-menu rerun remain in progress.
- Pushed `dd84051` to `codex/gemini-dual-provider-staging` and switched only
  the protected Render test service to that branch. The deploy succeeded and
  `/healthz` returned HTTP 200.
- Live provider checks confirmed Hunyuan 3.0 and COS are ready. Gemini is
  intentionally unavailable because `GEMINI_API_KEY` is absent; the customer
  UI now blocks that paid refinement path instead of pretending it is usable.
- Ran the exact 60-row mixed-rice workbook remotely. Upload, taxonomy, all six
  approved COS backgrounds, SHA verification, and selected-background binding
  passed. The first free sample failed after 75.961 seconds with a provider
  result-image TLS handshake timeout.
- Added a bounded retry only around the idempotent provider result-image GET.
  The paid generation submit is never retried, so the fix cannot duplicate a
  paid image request. Focused security/generation tests passed: `83 passed`.
- Pushed result-image retry commit `42a3739`; Render deployed it successfully.
- Re-ran the exact 60-row workbook. Upload, `mixed_rice` classification, all
  six approved COS backgrounds, selected `style-3`, and six paid Hunyuan free
  samples passed in 330.054 seconds. All six outputs preserved the selected
  background identity and output SHA verification.
- Visual QA rejected samples five and six even though structural checks passed:
  Chinese combo `烤排` became a western steak and the rice/choice semantics
  were wrong. This is a semantic-generation failure, not a provider, balance,
  COS, or selected-background failure.
- Canceled formal job `generation-956c3ed9aed687dffa4365d6` at 14/60 before
  accepting more wrong images. The test ledger charged 600 points, refunded
  600 points, netted zero, and restored the account balance to 1880.
- Root cause: `split_components` split inside square brackets before removing
  the bracket delimiters, dropping `烤肉` and leaving malformed `鸡排】`; the
  chroma prompt was also truncated at 250 characters and lacked Chinese
  mixed-rice/cutlet/choice semantics.
- Added a narrow bracket-component parser correction and prompt contract v2.
  It requires rice for rice categories only, disambiguates `烤排`/`鸡排`/
  `腿排`/`猪排`, enforces combo counts and one-of choices, forbids inset frames,
  and keeps the prompt below 600 characters. Non-rice categories have a
  regression guard. Focused verification passed: `48 passed`.
- Independent review rejected the earlier result-download retry as
  production-safe because a provider-controlled URL could target arbitrary
  hosts and the legacy replacement fallback could invoke a second paid model
  call after download failure.
- Restricted provider image downloads to credential-free HTTPS under the
  Tencent result-domain allowlist, rejected any non-public DNS answer, and
  revalidated every redirect. Retries now share a bounded wall-clock deadline.
- Added `ProviderResultDownloadError`; this failure is recorded but never
  enters the legacy paid text-to-image fallback. Focused security, generation,
  parser, and selected-background tests passed: `104 passed`.
- Complete regression after semantic and security fixes passed: `1374 passed,
  20 skipped`. The skips still require live PostgreSQL/Redis infrastructure.
- Second independent security review reproduced two remaining duplicate-cost
  paths: uppercase/malformed result URLs could escape the download sentinel,
  and an unknown TokenHub submit outcome automatically invoked the legacy
  cloud provider.
- All result decoding, downloading, and file-write failures now become
  `ProviderResultDownloadError`, including uppercase and malformed URL forms.
  The configured TokenHub route no longer falls back to another paid provider
  after any ambiguous outcome; its version token is
  `tokenhub-fail-closed-v2`.
- Focused fail-closed verification passed `115 passed`; final full regression
  passed `1375 passed, 20 skipped`.
- Final P1 review found directory creation and cleanup errors outside the
  paid-result sentinel. `save_result_image` now aggregates failures across
  directory creation, decode/download, save/replace, image close, and temporary
  cleanup; cleanup can neither overwrite the primary error nor trigger a paid
  fallback. Focused verification passed `116 passed`; full regression passed
  `1376 passed, 20 skipped`.
- Independent final P1 review passed for the default TokenHub staging route.
  Remaining P2 items are DNS-to-connection binding, strict resolver/body
  deadline enforcement, and durable provider-result recovery after all three
  GET attempts fail.
